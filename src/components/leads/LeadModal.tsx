'use client';

/** Legacy modal — staff lead recording is at /staff-portal/record-leads */
export const LeadModal: React.FC<{
  isOpen: boolean;
  onClose: () => void;
  leadToEdit?: unknown;
}> = () => null;
